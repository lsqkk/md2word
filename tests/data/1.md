1. **初始化森林**：将所有频率大于 0 的字符作为独立的叶节点，按 `(频率 << 8) | 字符序号` 排序，确保频率低且字母序小的节点优先参与合并；
2. **重复合并**：从森林中选取权值最小的两个节点，合并为一个新节点（权值为两者之和），将新节点放回森林：
   - 权值小的节点作为左孩子，权值大的节点作为右孩子；
   - 权值相等时，通过 `cmp_equal_weight` 函数进一步比较：叶节点优先于内部节点，均为叶节点时比较字符序号，均为内部节点时比较子树中的最小字符；
3. **直至森林只剩一个节点**，即为哈夫曼树的根节点。